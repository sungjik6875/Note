##### index.html

```html
<body>
  <h1>주문 배송 내역 작성서</h1>
  <div id="container">
    <form>
      <!-- Inputs for Billing -->
      <div class="formGroup">
        <h5> 주문 정보 </h5>
        <div class="inputArea">
          <label for="billingName">이름 : </label>
          <input type="text" id="billingName" required>
        </div>

        <div class="inputArea">
          <label for="billingTel">연락처 :</label>
          <input type="text" id="billingTel" required>
        </div>

        <div class="inputArea">
          <label for="billingAddr">주소 :</label>
          <input type="text" id="billingAddr">
        </div>
      </div>


      <!-- Inputs for Shipping -->
      <div class="formGroup">
          <h5> 배송 정보 </h5>
          <div class="inputArea">
            <label for="shippingName">이름 : </label>
            <input type="text" id="shippingName" required>
          </div>

          <div class="inputArea">
            <label for="shippingTel">연락처 :</label>
            <input type="text" id="shippingTel" required>
          </div>

          <div class="inputArea">
            <label for="shippingAddr">주소 :</label>
            <input type="text" id="shippingAddr" required>
          </div>
          <input id="check" type="checkbox">주문 정보와 배송 정보가 같습니다.</input>
      </div>

      <div id="payBtnContainer">
        <button id="payBtn">결제하기</button>
      </div>
    </form>
  </div>
  
  <script src="./js/action.js"></script>
</body>
```

> index 설명



##### style.css

```css
h1 {
  text-align: center;
}

.formGroup {
  border: 1px solid black;
  padding: 2px 20px 10px;
  margin: 3rem auto;
  max-width: 400px;
}

.inputArea {
  margin: 10px;
}

.inputArea input {
  min-width: 300px;
  float: right;
}

#check {
  float: left;
  margin-top: 10px;
  margin-left: 20px;
}

#payBtnContainer {
  max-width: 100px;
  margin: 0 auto;
}
```

> css 설명



##### action.js

```javascript
// 주문정보 > 배송정보 복사하기
const checkBox = document.forms[0].elements.check;
checkBox.addEventListener("click", function() {
  if (checkBox.checked === true) {
    document.querySelector("#shippingName").value
    = document.querySelector("#billingName").value;
    document.querySelector("#shippingTel").value
    = document.querySelector("#billingTel").value;
    document.querySelector("#shippingAddr").value
    = document.querySelector("#billingAddr").value;
  } else {
    document.querySelector("#shippingName").value = '';
    document.querySelector("#shippingTel").value = '';
    document.querySelector("#shippingAddr").value = '';  
  }
});
```

> action 설명